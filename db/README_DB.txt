This folder contains the SQLite database file `coach.db` (created automatically when server runs) and the schema is initialized by server.js.

Use the /api/logs POST endpoint to store {date, exercise, max_weight, max_reps} and GET to retrieve logs.
